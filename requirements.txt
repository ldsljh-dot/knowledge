# KnowledgeEngine 의존성
# pip install -r requirements.txt

# Tavily 검색 API 클라이언트
tavily-python>=0.3.0

# 환경변수 .env 파일 로드 (선택)
python-dotenv>=1.0.0

# RAG 청크 검색 (BM25)
rank-bm25>=0.2.2

# PDF 직접 파싱 (pdfplumber → pdfminer.six 기반)
# PDF URL 수집 시 Jina fallback으로 사용됨
pdfplumber>=0.10.0
