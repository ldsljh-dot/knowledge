`.agent/workflows/knowledge_tutor.md` 워크플로우를 읽고 단계별로 실행하세요.

실행 전 반드시 아래 순서를 따르세요:
1. 프로젝트 루트를 `git rev-parse --show-toplevel` 또는 `pwd`로 확인
2. `.env` 파일이 있으면 로드: `source .env`
3. `TAVILY_API_KEY` 환경변수 확인
4. `.agent/workflows/knowledge_tutor.md` 파일을 읽어 워크플로우 지시를 따름
